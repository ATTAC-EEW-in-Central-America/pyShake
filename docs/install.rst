Installation
============

At the command line:

.. code:: sh
    
    python -m pip install git+https://github.com/ATTAC-EEW-in-Central-America/pyShake.git

Uninstallation 
--------------

At the command line:

.. code:: sh
    
    python -m pip uninstall --verbose pyshake 